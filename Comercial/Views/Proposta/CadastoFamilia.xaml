<UserControl x:Class="Comercial.Views.Proposta.CadastoFamilia"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:Comercial.Views.Proposta" 
             xmlns:vm="clr-namespace:Comercial.Views.Proposta"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:CadastoFamiliaViewModel/>
    </UserControl.DataContext>

    <Grid>

        <telerik:RadGridView x:Name="rgFamilia" 
            telerik:StyleManager.Theme="Office2016" 
            AutoGenerateColumns="False"
            SelectionMode="Extended"
            ClipboardCopyMode="Cells, Header"
            ShowGroupPanel="False"
            ItemsSource="{Binding ComercialPropostaFamilias}" 
            SelectedItem="{Binding PropostaSelecionada, Mode=TwoWay}"
            NewRowPosition="Bottom"
            GroupRenderMode="Flat"
            CanUserFreezeColumns="False"
            RowEditEnded ="OnRowEditEnded"
            RowHeight="50" 
            CanUserDeleteRows="False">

            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding familia}" Header="Família" Width="auto" EnableDistinctValuesFiltering="True"/>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding resp_descricao}" Header="Resp Descrição" TextWrapping="Wrap" Width="180" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding resp_dimensao}" Header="Resp Dimensão" TextWrapping="Wrap" Width="180"/>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding resp_altera_descricao}" Header="Resp Altera Descrião" TextWrapping="Wrap" Width="180"/>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding resp_altera_dimensao}" Header="Resp Altera Dimensão" TextWrapping="Wrap" Width="180"/>
            </telerik:RadGridView.Columns>

        </telerik:RadGridView>

    </Grid>
    
</UserControl>
